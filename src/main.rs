mod matrix;
mod rng;
mod activation;
mod layer;
mod loss;
mod optimizer;
mod network;
mod data;
mod metrics;
mod io_utils;
mod init;

fn main() {
    println!("Fashion-MNIST Neural Network — Pure Rust, Std-Only");
    // TODO: Day 1 — wire up data loading + Matrix smoke test
}
